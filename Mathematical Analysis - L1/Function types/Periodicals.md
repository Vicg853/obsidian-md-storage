Periodical functions, can be defined that $\forall x, f(x + T) = f(x)$, where T is a fixed strictly positive integer, that represents f's variation period.

The sinus function is an example of a periodical function. 

